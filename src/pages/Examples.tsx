import { appContext } from "@/code/AppContextProvider";
import { useContext } from "react";

export default function ExamplesPage() {
    const { } = useContext(appContext);

    return (
        <main className="pad-content scroll-bar">
            test
        </main>
    );
}
